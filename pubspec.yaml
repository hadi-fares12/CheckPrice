name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  excel: ^2.0.7
  file_selector: ^1.0.3
  path_provider: ^2.0.15
  mobile_scanner: ^3.2.0
  shared_preferences: ^2.2.2
  share_plus: ^7.2.1
  permission_handler: ^11.3.1
  http: ^0.13.5  # ← MAKE SURE THIS IS ADDED
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  hive_generator: ^2.0.1
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true